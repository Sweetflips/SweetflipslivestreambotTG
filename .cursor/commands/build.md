Verify the Python project after substantive changes:

- Run `python -m compileall .` from the repo root (or narrow to edited packages).
- If tests exist, run `pytest` (or the project’s documented test command).
- Fix any syntax errors or failing tests before finishing.
