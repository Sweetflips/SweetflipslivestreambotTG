# Secrets and local overrides (never commit real passwords)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
config/streamer_agent.json
# Railway / env dumps (never commit)
config/railway-*.txt
config/env-config.txt
config/env-production.env
config/google-sheets-config.env

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
pretrained_models/

# Database (local SQLite etc.)
*.db
*.db-journal

# Credentials (repo-specific; do not ignore all *.json — we ship example JSON)
credentials/

# Logs
logs/
*.log
pids/
*.pid
*.seed
*.pid.lock

# Coverage
coverage/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
tmp/
temp/

# Local Docker CLI captures (not source)
docker_run_log.txt
compose_ps.txt
compose_build.txt
docker_images.txt
v.txt
